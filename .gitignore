# 会话产物（构建输出文件）
*.output

# 系统文件
.DS_Store
*.swp

# compose 环境变量（局域网 IP 等，避免误传本机信息）
.env

# 协程库构建产物（coro/build）
coro/build/

# 连接池构建产物（mysql_connection_pool/build）
mysql_connection_pool/build/

# superpowers 会话计划文档（本地临时生成）
docs/

build/

compile_commands.json